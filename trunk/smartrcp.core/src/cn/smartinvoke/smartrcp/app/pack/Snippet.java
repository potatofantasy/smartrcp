package cn.smartinvoke.smartrcp.app.pack;

public class Snippet {
	/**
		 * 将baseDir目录下的smartrcp程序文件压缩到saveDir目录下，
		 * zip文件名格式名如下：appName_appVersion.zip
		 * @param baseDir
		 * @param saveDir
		 * @param appName
		 * @param appVersion
		 * @param comment 该程序的注释
		 */
	    public void packFiles(String baseDir,String saveDir,String appName,String appVersion,String comment){
	       
	    }
	    /**
	     * 将程序zip文件appFile解压到
	     * @param appFile
	     */
	    public void install(String appFile){
	    	
	    }
}

